import styled from 'styled-components'

export const PostContainer = styled.div`
  padding: 2.5rem 2rem;
  overflow: hidden;

  ul,
  ol {
    margin-left: 1.25rem;
  }

  .line-break {
    white-space: pre-wrap;
  }
`
